import { AddItemForEconomy } from "../Functions";
import { ItemTesting } from "./Items";

export let Summit = (): void => {
  /* Add testing item */

  /* Ad boolean parameter for set to added item and ignore it */
  AddItemForEconomy(1, ItemTesting);
};
